const LAST_FRAME_IDX = 9;
const MAX_PLAYERS = 5;
const MAX_SCORE = 10;
const MIN_SCORE = 0;
const SPARE = "/";
const STRIKE = "X";

export { LAST_FRAME_IDX, MAX_PLAYERS, MAX_SCORE, MIN_SCORE, SPARE, STRIKE };
